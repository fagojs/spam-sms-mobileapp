import React from "react";
import { Ionicons } from "@expo/vector-icons";
import { orange } from "../../utils/color";

const AddButton = () => {
  return <Ionicons name="md-add-circle" size={75} color={orange} />;
};

export default AddButton;
